You will need to install npm vita and npm install firebase.
You will also need to install the regular frontend npm section.
You will also need to use all the code.
I had help from the firebase website, stack exchange, and other sources.
